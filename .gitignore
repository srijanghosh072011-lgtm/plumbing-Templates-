__pycache__/
*.pyc

# Next.js
node_modules/
.next/
out/
next-env.d.ts
.env*.local
.DS_Store

# Built site. Generated by the deploy workflow, which force-adds it
# (git add -f docs). Ignored here so it has exactly one writer — two
# writers to a generated directory means a conflict on every push, and
# resolving those conflicts once produced a published page containing
# markup from two different builds.
docs/
tsconfig.tsbuildinfo
